# Only the workflow actions. The integration declares no runtime requirements,
# and the test pins in requirements_test.txt track Home Assistant's own release
# cadence rather than an upstream dependency graph.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
    labels: [dependencies, ci]
